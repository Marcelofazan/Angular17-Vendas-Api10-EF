trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  npm_config_cache: $(Pipeline.Workspace)/.npm
  dotnetVersion: '10.0.x'

steps:
# ================= CONFIGURAÇÃO DE AMBIENTE =================
- task: UseNode@1
  inputs:
    version: '20.x'
  displayName: 'Ambiente: Instalar Node.js'

- task: UseDotNet@2
  inputs:
    version: '$(dotnetVersion)'
    packageType: 'sdk'
  displayName: 'Ambiente: Instalar .NET 10 SDK'

- task: Cache@2
  inputs:
    key: 'npm | "$(Agent.OS)" | Frontend/package.json'
    restoreKeys: |
      npm | "$(Agent.OS)"
    path: $(npm_config_cache)
  displayName: 'Garantir Cache do NPM'

# ================= PIPELINE DO FRONTEND (ANGULAR 17) =================
- script: npm install --legacy-peer-deps
  workingDirectory: '$(Build.SourcesDirectory)/Frontend'
  displayName: 'Frontend: Instalar dependências'

- script: npm run build -- --configuration production
  workingDirectory: '$(Build.SourcesDirectory)/Frontend'
  displayName: 'Frontend: Compilar o projeto Angular'

# ================= PIPELINE DO BACKEND (.NET 10) =================
# CORREÇÃO: Adicionado o Runtime -r win-x64 também no restore para mapear o ambiente do arquivo .exe
- task: DotNetCoreCLI@2
  displayName: 'Backend: Restaurar dependências'
  inputs:
    command: 'restore'
    projects: 'Backend/exemploAPIVendas.slnx'
    arguments: '-r win-x64'

# CORREÇÃO: Adicionado o Runtime -r win-x64 na compilação da solução
- task: DotNetCoreCLI@2
  displayName: 'Backend: Compilar API'
  inputs:
    command: 'build'
    projects: 'Backend/exemploAPIVendas.slnx'
    arguments: '--configuration $(buildConfiguration) -r win-x64 --no-restore'

- task: DotNetCoreCLI@2
  displayName: 'Backend: Executar testes'
  inputs:
    command: 'test'
    projects: 'Backend/exemploAPIVendas.slnx'
    arguments: '--configuration $(buildConfiguration) --no-build'
  condition: always()

# CORREÇÃO: Apontado diretamente para o .csproj da API principal para evitar o conflito de múltiplos outputs e gerar o exemploAPIVendas.exe limpamente
- task: DotNetCoreCLI@2
  displayName: 'Backend: Publicar para Deploy'
  inputs:
    command: 'publish'
    publishWebProjects: false
    projects: 'Backend/exemploAPIVendas/exemploAPIVendas.csproj'
    arguments: '--configuration $(buildConfiguration) -r win-x64 --self-contained false --output $(Build.ArtifactStagingDirectory)/ApiPublish --no-build'
    zipAfterPublish: false

# ================= SALVAR ARTEFATOS PARA DEPLOY =================
- script: |
    DIST_ROOT="$(Build.SourcesDirectory)/Frontend/dist"
    PROJECT_DIST=$(find "$DIST_ROOT" -mindepth 1 -maxdepth 2 -type d | head -n 1)
    if [ -z "$PROJECT_DIST" ]; then PROJECT_DIST="$DIST_ROOT"; fi
    echo "Diretório Completo de Dist do Angular detectado: $PROJECT_DIST"
    echo "##vso[task.setvariable variable=detectedAngularDist]$PROJECT_DIST"
  displayName: 'Mapear Pasta Raiz do Angular'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(detectedAngularDist)'
    ArtifactName: 'frontend-drop'
    publishLocation: 'Container'
  displayName: 'Publicar Artefato: Frontend'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/ApiPublish'
    ArtifactName: 'backend-drop'
    publishLocation: 'Container'
  displayName: 'Publicar Artefato: Backend'
